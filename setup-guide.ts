// SUILANCE Setup Guide and Dependencies

/*
Step-by-step guide to set up SUILANCE locally:

1. Clone the repository:
   git clone https://github.com/your-repo/suilance.git
   cd suilance

2. Install Node.js:
   Make sure you have Node.js version 14.x or later installed on your system.
   You can download it from https://nodejs.org/

3. Install dependencies:
   npm install

4. Set up environment variables:
   Create a .env.local file in the root directory and add necessary environment variables:
   NEXT_PUBLIC_API_URL=your_api_url
   NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

5. Run the development server:
   npm run dev

6. Open your browser and navigate to http://localhost:3000

Dependencies:

Below is a list of main dependencies you'll need for this project. These will be installed
automatically when you run `npm install`, but it's good to be aware of them:

1. Next.js: "^13.0.0"
   The React framework for production-grade applications.

2. React: "^18.2.0"
   A JavaScript library for building user inte norfaces.

3. React DOM: "^18.2.0"
   React package for working with the DOM.

4. TypeScript: "^4.8.4"
   A typed superset of JavaScript that compiles to plain JavaScript.

5. Tailwind CSS: "^3.2.0"
   A utility-first CSS framework.

6. shadcn/ui: (Various components)
   A collection of re-usable components built with Radix UI and Tailwind CSS.

7. Lucide React: "^0.144.0"
   A collection of simply beautiful open source icons.

8. Framer Motion: "^7.6.1"
   A production-ready motion library for React.

9. @mysten/sui.js: "^0.32.1"
   The Sui JavaScript SDK for interacting with the Sui blockchain.

10. Supabase.js: "^2.0.0"
    A client library for Supabase, an open source Firebase alternative.

11. Zod: "^3.19.1"
    TypeScript-first schema declaration and validation library.

12. React Hook Form: "^7.38.0"
    Performant, flexible and extensible forms with easy-to-use validation.

13. SWR: "^2.0.0"
    React Hooks library for data fetching.

14. date-fns: "^2.29.3"
    Modern JavaScript date utility library.

Development Dependencies:

1. ESLint: "^8.26.0"
   A tool for identifying and reporting on patterns found in ECMAScript/JavaScript code.

2. Prettier: "^2.7.1"
   An opinionated code formatter.

3. PostCSS: "^8.4.18"
   A tool for transforming CSS with JavaScript.

4. Autoprefixer: "^10.4.12"
   PostCSS plugin to parse CSS and add vendor prefixes to CSS rules.

Make sure to keep these dependencies up to date by regularly running `npm update`.
*/

// This file is for documentation purposes only and does not contain executable code.


